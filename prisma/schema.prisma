
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  // Conexión pooled (PgBouncer) — para queries en runtime
  url       = env("DATABASE_URL")
  // Conexión directa (sin pooling) — necesaria para `prisma migrate`
  directUrl = env("POSTGRES_URL_NON_POOLING")
}

model Contact {
  id        String   @id @default(cuid())
  nombre    String
  email     String
  telefono  String
  cedula    String?
  ciudad    String
  asunto    String?
  mensaje   String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("contacts")
}

model Quote {
  id            String   @id @default(cuid())
  nombre        String
  celular       String
  mail          String
  cedula        String
  ciudad        String
  source        String?
  selectedModel String
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  @@map("quotes")
}

model ServiceSubmission {
  id                 String   @id @default(cuid())
  nombre             String
  telefono           String
  ciudad             String
  cedulaRuc          String
  modelo             String
  email              String
  placa              String
  tipoServicioTaller String
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt

  @@map("service_submissions")
}
